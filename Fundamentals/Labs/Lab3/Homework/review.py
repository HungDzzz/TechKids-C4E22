# Why should we use functions at all?
    # nên sử dụng functions vì nó sẽ làm cho code trở nên gòn gàng hơn, có thể lôi ra sử dụng từ bất cứ đâu kể cả trong một file khác bằng cách import
# How to define/declare a function?
    # để xác định cũng như khai báo hàm ta dùng câu lệnh : def tenham(thamso): và cac câu lệnh trong hàm sẽ thụt đầu dòng để xác định thuộc hàm đó
# How to call/use a function?
    # để call or use 1 func ta phải gọi hàm đó vd : tenham()
# What is return, why and how do we use it?
    # return là để trả về giá trị cho 1 biến/thamso, ta sử dụng nó để cho bien/thamso đó thoát ra khỏi func và có thể dùng ở một chỗ khác ngoài func, cách dùng return tenbien
# Do we have to use return in every function?
    # chúng ta nên sử dụng return để có thể dùng biến đó tại một nơi khác khi cần
# What are function arguments/parameters, why and how we use it?
    # tham số trong hàm hình thành va đi cùng khi khởi tạo hàm, chung ta dung no để tham chiếu đến biến trong hàm
# How to use function from a different file other than our currently working file?
    # import tenham